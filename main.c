#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * main - prints whatever is passed as a variable argument list
 * @format: pointer to the string that should be printed including (conversion specifiers)  
 * Return: the number of characters printed if everything works fine
 */